let numeros = [9,-9,12,15,600];

for (let n = 0; n < numeros.length; n++) {
    if (numero[n] >= 1 && numero [n] <= 500
         && numeros[n]  % 2 !== 0
         && numeros[n] % 3 == 0 )
         {
            soma += numeros[n]

         }

}

document.write(soma);